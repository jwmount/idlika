<h1>New registry</h1>

<% form_for([@user, @registry]) do |f| %>
  <%= f.error_messages %>

  <%= render :partial=>'form', :locals => { :f => f } %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

