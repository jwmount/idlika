<div id="li_body">
  <%= render :partial => 'shared/sidebar' %>

<div id="li_content">
  <h1 class="about_this_gift"></h1>

<p>
  <%= link_to (image_tag @gift.photo.url(:large)), @gift.URL, :target=>'blank' %>
</p>
<p>
  <b>Gift Name:</b>
  <%=h @gift.name %>
</p>
<% unless @gift.registry.nil? %>
<p>
	<b>Gift Registry:</b>
	<%= @gift.registry.name %>
</p>
<% end %>
<p>
  <b>Gift Description:</b>
  <%=h @gift.description %>
</p>

<% if !session[:friend].nil? %>
  <%= link_to 'I want one too!', edit_user_gift_path( @user, @gift) %> | 
<% end %>

<% if @user == current_user %>
  <%= link_to 'Edit', edit_user_gift_path(@user,@gift) %> |
  <%= link_to 'Delete', gift_path(@gift),
         :confirm => "Are you sure you want to delete #{@gift.name} from #{@gift.registry.name}?", 
         :method => :delete %> 
<% end %>

<% flash["idlik12"] = @gift.id %>
<!-- button_to "I'd like one too", idlik12_path -->

<!-- I'LL GET THIS appears only when visiting someone else's list -->
<%= link_to( "I'll Get This Gift for #{@user.username}", root_url) unless @user.id == current_user[:id] %>

<br />
<!-- link_to 'Back', gifts_path -->

</div><!-- li_content -->

<div class="clearer"></div>
</div><!-- li_body -->
